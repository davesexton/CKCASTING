- @title = 'Cast Book'
- @description = 'View our current rosta of cast members online'

%div.pageDesc
  %p
    Our cast book contains all current available cast members, use the page
    numbers below to step through each page. Just click on a cast member's
    image to see their details. Use the filter section on the left-hand
    side of the page to narrow your search by the available criteria.

%div.columnFilter
  %fieldset
    %legend Filter by
    %a.reset Reset all filters
    %form#castFilters
      %div.filterLabel
        Gender:
        - @gender_group.each do |k, v|
          %div.filterOption
            %input{type: 'checkbox', name: 'gender[]', value: k, id: "gender_#{k}"}
            %label{for: "gender_#{k}"} #{v}

      %div.filterLabel Family Group:
      %div.filterOption
        %input#family{type: 'checkbox', name: 'family[]', value: true}
        %label{for: 'family'} In family group

      %div.filterLabel
        Age Group:
        - @age_group.each do |k, v|
          %div.filterOption
            %input{type: 'checkbox', name: 'age[]', value: k, id: "age_#{k}"}
            %label{for: "age_#{k}"} #{v}
            %br/

      %div.filterLabel
        Height Group:
        - @height_group.each do |k, v|
          %div.filterOption
            %input{type: 'checkbox', name: 'height[]', value: k, id: "height_#{k}"}
            %label{for: "height_#{k}"} #{v}
            %br/

      %div.filterLabel
        Hair Colour:
        - @hair_group.each do |k, v|
          %div.filterOption
            %input{type: 'checkbox', name: 'hair[]', value: k, id: "hair_#{k}"}
              %label{for: "hair_#{k}"} #{v}
            %br/

      %div.filterLabel
        Eye Colour:
        - @eye_group.each do |k, v|
          %div.filterOption
            %input{type: 'checkbox', name: 'eye[]', value: k, id: "eye_#{k}"}
              %label{for: "eye_#{k}"} #{v}
            %br/

      = hidden_field_tag 'page', @page, {value: 1}

%div.columnCastList
  %div#castThumbs
    = render partial: 'shared/castlist', locals: {castlist: @castlist}
