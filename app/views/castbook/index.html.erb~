<div class="columnFilter">
  <fieldset>
    <legend>Filter by</legend>
    Gender:<br />
    <% @gender_group.each do |k, v|  %>
    <input type="checkbox" name="gender" id="<%= k %>" value="<%= k %>" />
    <label for="<%= k %>"><%= "#{k}(#{v})" %></label><br />
    <% end %>
    <hr />
    
  </fieldset>
</div>


<div class="columnCastList">


  <table>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>

  <% @castbook.each do |castbook| %>
    <tr>
      <td><%= castbook.full_name %></td>
      <td><%= castbook.height_group %></td>
      <td><%= castbook.height_feet %></td>
      <td><%= link_to 'Show', castbook %></td>
   </tr>
  <% end %>
  </table>
</div>

